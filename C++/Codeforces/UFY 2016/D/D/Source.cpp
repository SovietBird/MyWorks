#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main() {

	string A, B;

	long k = 0;

	//cin >> A;
	//cin >> B;

	//long n = max(B.size(), A.size());

	/*for (int i = 0; i < n; i++) {

		if ((i <= A.size()) && (A[i] == 'C'))
			k++;

		if ((i <= B.size()) && (B[i] == 'C'))
			k++;
	}*/

	cout << 0;

	return 0;
}